<!DOCTYPE html>
<html lang="ka">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>უსაფრთხოების კითხვა</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h2>უსაფრთხოების შემოწმება</h2>
        
        <% if (error) { %>
            <div class="error-message"><%= error %></div>
        <% } %>

        <form action="/security-question" method="POST">
            <div class="form-group">
                <label>კითხვა: <strong><%= questionText %></strong></label>
            </div>
            
            <div class="form-group">
                <label for="answer">თქვენი პასუხი</label>
                <input type="text" id="answer" name="answer" required autocomplete="off">
            </div>

            <button type="submit">შემოწმება</button>
        </form>
    </div>
</body>
</html>
