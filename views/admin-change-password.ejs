<!DOCTYPE html>
<html lang="ka">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>პაროლის შეცვლა (Admin)</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h2>პაროლის შეცვლა მომხმარებლისთვის: <%= targetUser.username %></h2>
        <p>Email: <%= targetUser.email %></p>

        <% if (error) { %>
            <div class="error-message"><%= error %></div>
        <% } %>

        <form action="/admin/change-password" method="POST">
            <input type="hidden" name="targetEmail" value="<%= targetUser.email %>">
            
            <div class="form-group">
                <label for="newPassword">ახალი პაროლი</label>
                <input type="password" id="newPassword" name="newPassword" required minlength="8">
            </div>

            <div class="form-group">
                <label for="confirmPassword">გაიმეორეთ ახალი პაროლი</label>
                <input type="password" id="confirmPassword" name="confirmPassword" required minlength="8">
            </div>

            <button type="submit">შეცვლა</button>
        </form>
        
        <div style="margin-top: 15px; text-align: center;">
            <a href="/admin" class="link">უკან ადმინ პანელში</a>
        </div>
    </div>
</body>
</html>
