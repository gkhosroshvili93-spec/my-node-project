<!DOCTYPE html>
<html lang="ka">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results - <%= query %></title>
    <link href="https://fonts.googleapis.com/css2?family=Segoe+UI:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body { margin: 0; font-family: 'Segoe UI', sans-serif; background-color: #f0f2f5; color: #050505; }
        
        .navbar { background: white; height: 60px; padding: 0 20px; box-shadow: 0 1px 2px rgba(0,0,0,0.1); display: flex; align-items: center; justify-content: space-between; position: sticky; top: 0; z-index: 100; }
        .nav-logo { font-size: 24px; font-weight: bold; color: #1877f2; text-decoration: none; }
        .nav-search input { background: #f0f2f5; border: none; padding: 10px 15px; border-radius: 20px; width: 250px; font-size: 15px; }
        .nav-icons { display: flex; gap: 10px; }
        .nav-icon { width: 40px; height: 40px; background: #e4e6eb; border-radius: 50%; display: flex; align-items: center; justify-content: center; text-decoration: none; color: black; font-size: 20px; }

        .container { max-width: 800px; margin: 20px auto; padding: 20px; }
        
        .section-title { font-size: 20px; font-weight: bold; margin-bottom: 15px; color: #65676b; border-bottom: 1px solid #e4e6eb; padding-bottom: 10px; }
        
        .result-card { background: white; padding: 15px; border-radius: 8px; box-shadow: 0 1px 2px rgba(0,0,0,0.1); margin-bottom: 10px; display: flex; align-items: center; gap: 15px; }
        .user-avatar { width: 60px; height: 60px; border-radius: 50%; object-fit: cover; }
        .user-info { flex: 1; }
        .user-name { font-weight: bold; font-size: 16px; margin-bottom: 5px; }
        .user-username { color: #65676b; font-size: 14px; }
        
        .action-btn { background: #e4e6eb; border: none; padding: 8px 15px; border-radius: 6px; font-weight: 600; cursor: pointer; text-decoration: none; color: black; font-size: 14px; }
        .action-btn:hover { background: #d8dadf; }

        .post-content { font-size: 15px; color: #050505; margin-top: 5px; }
        .post-meta { font-size: 12px; color: #65676b; margin-bottom: 5px; }
    </style>
</head>
<body>

    <div class="navbar">
        <a href="/dashboard" class="nav-logo" style="display: flex; align-items: center; gap: 2px;">
            <span style="color: #ff4757; font-weight: 800;">Bta</span>SocialPortal
        </a>
        
        <form action="/search" method="GET" class="nav-search">
            <input type="text" name="q" placeholder="Search Portal..." value="<%= query %>">
        </form>

        <div class="nav-icons">
            <a href="/dashboard" class="nav-icon" title="Home">🏠</a>
            <a href="/friends" class="nav-icon" title="Friends" style="position:relative;">
                👥
                <% if (typeof friendRequestsCount !== 'undefined' && friendRequestsCount > 0) { %>
                    <span style="position: absolute; top: -5px; right: -5px; background: #e41e3f; color: white; border-radius: 50%; width: 18px; height: 18px; font-size: 11px; display: flex; align-items: center; justify-content: center; border: 2px solid white;">+<%= friendRequestsCount %></span>
                <% } %>
            </a>
            <a href="/chat" class="nav-icon" title="Messenger">💬</a>
        </div>
    </div>

    <div class="container">
        <h2>ძებნის შედეგები: "<%= query %>"</h2>

        <div class="section-title">მომხმარებლები (<%= users.length %>)</div>
        <% if (users.length === 0) { %>
            <p style="color:#65676b;">მომხმარებლები არ მოიძებნა</p>
        <% } %>
        
        <% users.forEach(u => { %>
            <div class="result-card">
                <img src="<%= u.avatar || '/uploads/default.png' %>" class="user-avatar">
                <div class="user-info">
                    <div class="user-name">
                        <a href="/user/<%= u.email %>" style="text-decoration:none; color:inherit;"><%= u.firstName %> <%= u.lastName %></a>
                    </div>
                    <div class="user-username">@<%= u.username %></div>
                </div>
                
                <% if (currentUser.blocked && currentUser.blocked.includes(u.email)) { %>
                    <form action="/user/unblock" method="POST">
                        <input type="hidden" name="targetEmail" value="<%= u.email %>">
                        <button type="submit" class="action-btn" style="background:#31a24c; color:white;">Unblock</button>
                    </form>
                <% } else { %>
                    <div style="display:flex; gap:10px;">
                         <a href="/chat?with=<%= u.email %>" class="action-btn">Message</a>
                         <form action="/friends/add" method="POST">
                            <input type="hidden" name="targetEmail" value="<%= u.email %>">
                            <button type="submit" class="action-btn" style="background:#1877f2; color:white;">Add Friend</button>
                         </form>
                         <!-- Block Button -->
                         <form action="/user/block" method="POST" onsubmit="return confirm('Are you sure you want to block this user?');">
                            <input type="hidden" name="targetEmail" value="<%= u.email %>">
                            <button type="submit" class="action-btn" style="background:#d63031; color:white;">Block</button>
                         </form>
                    </div>
                <% } %>
            </div>
        <% }) %>

        <div class="section-title" style="margin-top: 30px;">პოსტები (<%= posts.length %>)</div>
        <% if (posts.length === 0) { %>
            <p style="color:#65676b;">პოსტები არ მოიძებნა</p>
        <% } %>

        <% posts.forEach(p => { %>
            <div class="result-card" style="display:block;">
                <div class="post-meta">
                    <strong><%= p.authorName %></strong> - <%= new Date(p.timestamp).toLocaleDateString('ka-GE') %>
                </div>
                <div class="post-content"><%= p.content %></div>
                <div style="margin-top:10px;">
                    <a href="/dashboard" style="font-size:13px; color:#1877f2; text-decoration:none;">View in Feed</a>
                </div>
            </div>
        <% }) %>

    </div>

</body>
</html>
